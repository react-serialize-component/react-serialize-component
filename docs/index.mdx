---
name: My Document
route: /custom-route
menu: Documents
---
import { Playground } from 'docz'

# Counter


<Playground>
  {() => {
    const [counter, setCounter] = React.useState(0)
    return (
      <>
        <button onClick={() => setCounter(counter => counter+1)}>
          Increase
        </button>
        <p>{counter}</p>
      </>
    )
  }}
</Playground>


``` json schema=true
{
  type: 'page'
}
```


``` tsx live=true noInline=true
  const a = 1;
  render(<h3>
    So functional. Much wow!
  </h3>)
```
